# Supabase – get these from https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Google Fast Login (One Tap + Sign in with Google). Create a Web application OAuth 2.0 Client ID at
# https://console.cloud.google.com/apis/credentials, add your Site URL and /auth/callback to Authorized redirect URIs,
# and enable the Google provider in Supabase Auth (Authentication → Providers). If unset, email/password only.
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Canonical site URL for SEO (sitemap, robots, OpenGraph, canonical links). Use in production.
# NEXT_PUBLIC_SITE_URL=https://yoursite.com

# Resend: required for sending application notification emails to employers. Get key at https://resend.com
# If unset, applications are still saved but no email is sent.
RESEND_API_KEY=re_iMvTjFno_EhUR4x386aCV8ypJE3GcxtkP
# RESEND_FROM=applications@yourdomain.com
